[Service]
# StickLab OS live session: autologin root on tty1 (standard live-ISO pattern;
# installed systems should create a user + lock this down via archinstall).
ExecStart=
ExecStart=-/sbin/agetty -o '-p -f -- \\u' --noclear --autologin root - $TERM
